name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]
  workflow_dispatch:  # Allow manual triggers

jobs:
  lint:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - name: Install dependencies
        run: pip install ruff
      - name: Lint with ruff
        run: ruff check .

  typecheck:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"
      - name: Install system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y libgirepository-1.0-dev libcairo2-dev gir1.2-glib-2.0 gobject-introspection pkg-config python3-gi libegl1 libxkbcommon0
      - name: Install dependencies
        run: |
          pip install -e ".[dev]"
          pip install mypy types-setuptools
      - name: Type check with mypy
        run: mypy --ignore-missing-imports --no-error-summary apps/ services/ crates/ tools/ || echo "::warning::mypy found type errors (non-blocking)"

  test:
    runs-on: ubuntu-latest
    strategy:
      matrix:
        python-version: ["3.10", "3.11", "3.12"]
    steps:
      - uses: actions/checkout@v4
      - name: Set up Python ${{ matrix.python-version }}
        uses: actions/setup-python@v5
        with:
          python-version: ${{ matrix.python-version }}
      - name: Install system dependencies
        run: |
          sudo apt-get update
          sudo apt-get install -y libgirepository-1.0-dev libcairo2-dev gir1.2-glib-2.0 gobject-introspection pkg-config python3-gi libegl1 libxkbcommon0
      - name: Install dependencies
        run: |
          pip install -e ".[dev]"
          pip install pytest pytest-cov
      - name: Run tests with coverage
        run: |
          QT_QPA_PLATFORM=offscreen pytest -v --cov=apps --cov=services --cov=crates --cov=tools --cov-report=xml \
            --ignore=tests/test_gui_widgets.py \
            --ignore=tests/test_openrazer_bridge.py \
            --ignore=tests/test_app_watcher.py \
            --ignore=tests/test_hotkey_backends.py
      - name: Upload coverage to Codecov
        if: matrix.python-version == '3.11'
        uses: codecov/codecov-action@v4
        with:
          files: coverage.xml
          fail_ci_if_error: false
          token: ${{ secrets.CODECOV_TOKEN }}  # Optional for public repos

